# DAQ_SYNC W1 规格条目对照表

2026-09-01。工单 W1。引擎 4.1。

## 任务 A

- A-1 产物与打印通道名去写死：已做。`timeAudit`、`meta.note`、`fprintf`、corrected/intermediate 变量名按拓扑通道键。局部变量 `C5U` 等未改。
- A-2 等价输出隔离：已做。`_acceptance/T4.x/equiv/`。
- A-3 验收读 `.derivations.target`：已做。去掉 `BR6U`/`C6U` 候选列表。
